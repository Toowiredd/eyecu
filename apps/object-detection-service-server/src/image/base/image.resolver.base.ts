/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Image } from "./Image";
import { ImageCountArgs } from "./ImageCountArgs";
import { ImageFindManyArgs } from "./ImageFindManyArgs";
import { ImageFindUniqueArgs } from "./ImageFindUniqueArgs";
import { CreateImageArgs } from "./CreateImageArgs";
import { UpdateImageArgs } from "./UpdateImageArgs";
import { DeleteImageArgs } from "./DeleteImageArgs";
import { DetectedObjectFindManyArgs } from "../../detectedObject/base/DetectedObjectFindManyArgs";
import { DetectedObject } from "../../detectedObject/base/DetectedObject";
import { ObjectCountFindManyArgs } from "../../objectCount/base/ObjectCountFindManyArgs";
import { ObjectCount } from "../../objectCount/base/ObjectCount";
import { ImageService } from "../image.service";
@graphql.Resolver(() => Image)
export class ImageResolverBase {
  constructor(protected readonly service: ImageService) {}

  async _imagesMeta(
    @graphql.Args() args: ImageCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Image])
  async images(@graphql.Args() args: ImageFindManyArgs): Promise<Image[]> {
    return this.service.images(args);
  }

  @graphql.Query(() => Image, { nullable: true })
  async image(
    @graphql.Args() args: ImageFindUniqueArgs
  ): Promise<Image | null> {
    const result = await this.service.image(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Image)
  async createImage(@graphql.Args() args: CreateImageArgs): Promise<Image> {
    return await this.service.createImage({
      ...args,
      data: {
        ...args.data,

        detectedObject: args.data.detectedObject
          ? {
              connect: args.data.detectedObject,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => Image)
  async updateImage(
    @graphql.Args() args: UpdateImageArgs
  ): Promise<Image | null> {
    try {
      return await this.service.updateImage({
        ...args,
        data: {
          ...args.data,

          detectedObject: args.data.detectedObject
            ? {
                connect: args.data.detectedObject,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Image)
  async deleteImage(
    @graphql.Args() args: DeleteImageArgs
  ): Promise<Image | null> {
    try {
      return await this.service.deleteImage(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [DetectedObject], { name: "detectedObjects" })
  async findDetectedObjects(
    @graphql.Parent() parent: Image,
    @graphql.Args() args: DetectedObjectFindManyArgs
  ): Promise<DetectedObject[]> {
    const results = await this.service.findDetectedObjects(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [ObjectCount], { name: "objectCounts" })
  async findObjectCounts(
    @graphql.Parent() parent: Image,
    @graphql.Args() args: ObjectCountFindManyArgs
  ): Promise<ObjectCount[]> {
    const results = await this.service.findObjectCounts(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => DetectedObject, {
    nullable: true,
    name: "detectedObject",
  })
  async getDetectedObject(
    @graphql.Parent() parent: Image
  ): Promise<DetectedObject | null> {
    const result = await this.service.getDetectedObject(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
